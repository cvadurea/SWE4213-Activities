
export default function MyButton({ onClick }) {
  return (
    // TODO 5: Create button component with the correct onClick that says "Submit Form"
    <button onClick={onClick}>Submit Form</button>
  );
}
